? What does Column level security controls:

? Allow only users(suzy) from accounting group to see only column(employeeid, lastname, firstname, title) from employees table.
- Check whether user can select all columns in the table
- Restrict all users from selecting columns in all tables in public schema.
- Allow only restricted columns 
    - Try all colums -> fail
    - Try selected column -> success

? Allow only users from sales group to see only column(contactname, contacttitle, phone) from customer table.

? Show roles having list of privelage in employee table on columns.