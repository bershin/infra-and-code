? What does instance level security controls

? Permissions applied in instance level

? PostgreSQL privileges are additive (permissive), not restrictive, explain

? Create a role and add users to it who can login.
- Create two roles "accounting" and "hr"(group) which doesn't have any permission and can't login.
    - Check the permission granted to them using sql query.
    - try login as hr or accounting, it should fails
- Create suzy with just login permission and set password.
    - Check the permission granted to them using sql query.
    - try login, should succeed.
- Create bobby with just login permission and set password.
    - Check the permission granted to them using sql query.
    - try login, should succeed.
- By default newly created users should not be able to connect to other databases unless granted.
    - Check whether all users has connect permission to database using sql query.
    - Restrict access to all users to connect to northwind database when login.
    - Test login after -> fails
- Add user to group/rle to inherit permission fro role.
    - suzy to accounting & bobby to hr.
    - Check role and its members using sql query.

? Create sales role and add jill to the role

? Change password for suzy

? Allow northwind database to be connected back by all users.
? Allow northwind database to be connected back by all users and able to create schemas inside the database

? Remove john from hr role

? By default, roles are INHERIT, Create dba role that do not let users to inherit permission, but has permission to create database: 
- Add john user to dba role and check he can't create db. -> fails
- Temporarily switches your active role to hr for the current session and create db -> success

? Remove a role

