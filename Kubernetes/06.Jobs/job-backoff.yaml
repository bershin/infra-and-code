apiVersion: batch/v1
kind: Job
metadata:
  name: job-backoff
spec:
  template:
    spec:
      containers:
      - name: counter
        image: alpine
        command: ['sh', '-c', 'echo Backoff limits; exit 1']
      restartPolicy: Never
  backoffLimit: 4