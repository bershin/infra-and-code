apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: noaffinity-router
  annotations:
    kubernetes.io/ingress.class: "gce"
    external-dns.alpha.kubernetes.io/hostname: appnoaffinity.bjohn.online
spec:
  defaultBackend:
    service:
      name: noaffinity-service
      port:
        number: 80

---

