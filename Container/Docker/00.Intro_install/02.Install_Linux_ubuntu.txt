Install Docker desktop - Based on Linux
- https://www.docker.com/products/docker-desktop/
    - Script (or)
    - Step by step
$ docker --version
$ docker version
$ sudo docker run hello-world

$ sudo groupadd docker
$ sudo useradd -aG docker $USER
$ newgrp docker 
$ docker run hello-world